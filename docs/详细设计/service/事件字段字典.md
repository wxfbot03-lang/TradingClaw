# TradingClaw 事件字段字典

## 1. 文档说明

- 本文档用于统一 `docs/详细设计/service/` 下事件模型相关字段口径。
- 目标是保证各模块在设计领域事件、集成事件、工作流事件时，字段命名、类型和语义保持一致。
- 本文档补充 `API字段字典.md`，重点覆盖事件信封、事件 payload、分区与路由、回放与审计相关字段。

## 1.1 相关文档

- 总体总览：`docs/详细设计/service/后端详细设计.md`
- API 字段字典：`docs/详细设计/service/API字段字典.md`
- 错误码字典：`docs/详细设计/service/错误码字典.md`
- 状态字段枚举表：`docs/详细设计/service/状态字段枚举表.md`
- 用户与账户：`docs/详细设计/service/用户与账户详细设计.md`
- 行情与资讯：`docs/详细设计/service/行情与资讯详细设计.md`
- 交易网关：`docs/详细设计/service/交易网关详细设计.md`
- 策略系统：`docs/详细设计/service/策略系统详细设计.md`
- 智能能力与治理：`docs/详细设计/service/智能能力与治理详细设计.md`
- 风控审计与通知：`docs/详细设计/service/风控审计与通知详细设计.md`

## 2. 事件设计原则

- 事件字段统一使用 `snake_case`。
- 事件必须区分“信封字段”和“业务 payload 字段”。
- 同一业务事实只允许一个主发布方。
- 同一聚合根相关事件必须具备稳定分区键，避免乱序消费。
- 事件总线平台必须在实现前冻结；推荐采用 Kafka + Schema Registry 作为主事实事件总线，默认交付语义按 at-least-once 设计并通过幂等消费实现最终一致。

## 3. 事件信封字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `event_id` | string | 是 | 事件唯一标识 |
| `event_type` | string | 是 | 事件类型，如 `order.created` |
| `event_version` | integer | 是 | 事件 schema 版本 |
| `occurred_at` | string(datetime) | 是 | 业务事实发生时间 |
| `published_at` | string(datetime) | 是 | 消息发布时间 |
| `producer` | string | 是 | 发布服务标识 |
| `trace_id` | string | 否 | 链路追踪 ID |
| `request_id` | string | 否 | 发起请求 ID |
| `workflow_id` | string | 否 | 关联工作流 ID |
| `aggregate_type` | string | 是 | 聚合根类型，如 `order`、`strategy` |
| `aggregate_id` | string | 是 | 聚合根 ID |
| `tenant_id` | string | 否 | 多租户预留字段 |
| `partition_key` | string | 是 | 分区键 |
| `payload` | object | 是 | 业务负载 |
| `meta` | object | 否 | 扩展元数据 |

## 4. 事件元数据字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `meta.retry_count` | integer | 已重试次数 |
| `meta.source_env` | string | 来源环境，如 `prod`、`staging` |
| `meta.schema_name` | string | schema 名称 |
| `meta.schema_ref` | string | schema 引用或版本地址 |
| `meta.replay` | boolean | 是否来自回放 |
| `meta.dlq` | boolean | 是否来自死信队列 |
| `meta.reason_code` | string | 与失败相关的结构化原因码 |

## 5. 路由与分区字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `partition_key` | string | 用于 topic 分区与顺序消费 |
| `aggregate_type` | string | 聚合根类型 |
| `aggregate_id` | string | 聚合根唯一 ID |
| `producer` | string | 发布方服务名称 |
| `event_type` | string | 事件类型，用于消费者路由 |

分区建议：

- 用户事件按 `user_id`
- 账户事件按 `account_id`
- 行情事件按 `instrument_id`
- 订单事实事件按 `order_id`
- `execution.report_received` 按 `order_id`
- 与订单恢复相关的 `risk.review_completed` 按 `order_id`
- 策略事件按 `strategy_instance_id`

## 6. 身份与账户事件字段

### 6.1 `user.registered`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `user_id` | string | 用户 ID |
| `identity_type` | string | 注册身份类型 |

### 6.2 `session.created`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `session_id` | string | 会话 ID |
| `user_id` | string | 用户 ID |
| `expires_at` | string(datetime) | 过期时间 |

### 6.3 `session.expired`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `session_id` | string | 会话 ID |
| `user_id` | string | 用户 ID |
| `expires_at` | string(datetime) | 过期时间 |
| `reason` | string | 失效原因 |

### 6.4 `account.bound`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `account_id` | string | 账户 ID |
| `user_id` | string | 用户 ID |
| `account_type` | string | 账户类型 |

### 6.5 `account.capability_changed`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `account_id` | string | 账户 ID |
| `user_id` | string | 用户 ID |
| `capabilities` | object | 最新能力矩阵投影 |
| `account_capability_status` | string | 账户能力可用状态 |

发布约束：仅允许 `account-service` 发布。

### 6.6 `default_session.changed`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `account_id` | string | 账户 ID |
| `user_id` | string | 用户 ID |
| `default_trading_session_id` | string | 默认交易会话 ID |

发布约束：仅允许账户域在目标会话已处于 `AVAILABLE` 时发布。

## 7. 行情与资讯事件字段

### 7.1 `quote.updated`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `instrument_id` | string | 标的 ID |
| `symbol` | string | 标的代码 |
| `market` | string | 市场代码 |
| `price` | number | 最新价 |
| `timestamp` | string(datetime) | 行情时间 |

### 7.2 `historical.backfilled`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `instrument_id` | string | 标的 ID |
| `range.start_at` | string(datetime) | 补齐起点 |
| `range.end_at` | string(datetime) | 补齐终点 |
| `period` | string | 周期 |

### 7.3 `indicator.calculated`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `instrument_id` | string | 标的 ID |
| `indicator_type` | string | 指标类型 |
| `window` | integer | 指标窗口 |
| `calculated_at` | string(datetime) | 计算时间 |

### 7.4 `news.ingested`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `news_id` | string | 资讯 ID |
| `source_id` | string | 来源 ID |
| `published_at` | string(datetime) | 发布时间 |

### 7.5 `news.scored`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `news_id` | string | 资讯 ID |
| `score` | number | 影响力分数 |
| `scored_at` | string(datetime) | 评分时间 |

## 8. 交易事件字段

### 8.1 `order.created`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `order_id` | string | 统一订单 ID |
| `user_id` | string | 用户 ID |
| `account_id` | string | 账户 ID |
| `strategy_instance_id` | string | 关联策略实例，可空 |
| `asset_class` | string | 资产类型 |
| `instrument_id` | string | 标的统一唯一标识 |
| `instrument.symbol` | string | 标的代码 |
| `instrument.market` | string | 市场代码 |
| `side` | string | 买卖方向 |
| `order_type` | string | 订单类型 |
| `price` | number | 下单价格 |
| `quantity` | number | 下单数量 |
| `idempotency_key` | string | 幂等键 |

### 8.2 `order.accepted`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `order_id` | string | 统一订单 ID |
| `channel_order_id` | string | 通道订单号 |

### 8.3 `order.review_required`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `order_id` | string | 统一订单 ID |
| `risk_event_id` | string | 风险事件 ID |
| `reason_code` | string | 原因码，通常为 `RSK-REVIEW-001` |

### 8.4 `order.rejected`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `order_id` | string | 统一订单 ID |
| `reason_code` | string | 拒绝原因码 |

### 8.5 `order.partially_filled` / `order.filled`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `order_id` | string | 统一订单 ID |
| `filled_quantity` | number | 已成交数量 |
| `avg_fill_price` | number | 平均成交价 |

### 8.6 `execution.report_received`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `channel_order_id` | string | 通道订单号 |
| `order_id` | string | 统一订单 ID |
| `raw_status` | string | 原始通道状态 |
| `account_id` | string | 账户 ID |
| `occurred_at` | string(datetime) | 回报发生时间 |

分区约束：`partition_key` 必须等于 `order_id`。

### 8.7 `trading_session.created` / `trading_session.available` / `trading_session.expired` / `trading_session.closed`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `trading_session_id` | string | 交易会话 ID |
| `user_id` | string | 用户 ID |
| `account_id` | string | 账户 ID |
| `trading_session_status` | string | 交易会话状态 |
| `expires_at` | string(datetime) | 过期时间，可空 |
| `reason` | string | 失效原因，可空 |

发布约束：仅允许 `trade-gateway-service` 发布。

### 8.8 `broker_session.updated`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `broker_session_id` | string | 券商会话 ID |
| `account_id` | string | 账户 ID |
| `adapter_session_status` | string | 适配层会话状态映射结果 |
| `expires_at` | string(datetime) | 过期时间，可空 |

### 8.9 `exchange_session.updated`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `session_ref` | string | 交易所会话引用 |
| `account_id` | string | 账户 ID |
| `adapter_session_status` | string | 适配层会话状态映射结果 |
| `expires_at` | string(datetime) | 过期时间，可空 |

### 8.10 `exchange_credential.validated`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `account_id` | string | 账户 ID |
| `valid` | boolean | 凭据是否有效 |
| `capabilities` | object | 原始能力矩阵 |
| `account_capability_status` | string | 原始账户能力状态 |

### 8.11 `broker_account.validated`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `account_id` | string | 账户 ID |
| `broker_code` | string | 券商编码 |
| `valid` | boolean | 券商侧账户校验结果 |
| `capabilities` | object | 原始能力矩阵 |
| `account_capability_status` | string | 原始账户能力状态 |

## 9. 策略事件字段

### 9.1 `strategy.created`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `strategy_instance_id` | string | 策略实例 ID |
| `strategy_type` | string | 策略类型 |
| `account_id` | string | 绑定账户 |

### 9.2 `strategy.started` / `strategy.stopped`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `strategy_instance_id` | string | 策略实例 ID |
| `workflow_id` | string | 关联工作流 ID |
| `reason` | string | 停止原因，启动时可空 |

### 9.3 `strategy.snapshot_saved`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `strategy_instance_id` | string | 策略实例 ID |
| `snapshot_version` | integer | 快照版本 |

### 9.4 `strategy.signal.generated`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `strategy_instance_id` | string | 策略实例 ID |
| `strategy_type` | string | 策略类型 |
| `signal_id` | string | 信号 ID |
| `signal_type` | string | 信号类型 |
| `market_snapshot_ref` | string | 市场快照引用 |
| `signal_payload` | object | 信号内容 |

### 9.5 `trading_plan.generated`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `plan_id` | string | 交易计划 ID |
| `strategy_instance_id` | string | 策略实例 ID |
| `plan_payload` | object | 计划内容 |

## 10. 智能能力事件字段

### 10.1 `conversation.created`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `conversation_id` | string | 会话 ID |
| `user_id` | string | 用户 ID |

### 10.2 `tool_call.executed`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `conversation_id` | string | 会话 ID |
| `tool_name` | string | 工具名称 |
| `status` | string | 调用结果状态 |
| `reason_code` | string | 失败原因码，可空 |

### 10.3 `ocr.job_finished`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `job_id` | string | OCR 任务 ID |
| `status` | string | OCR 状态 |
| `text` | string | 识别文本，可空 |
| `confidence` | number | 置信度，可空 |

## 11. 风控、审计与通知事件字段

### 11.1 `risk.rule_triggered`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `rule_code` | string | 风控规则编码 |
| `resource_ref` | string | 资源引用 |
| `severity` | string | 风险等级 |

### 11.2 `risk.review_required`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `risk_event_id` | string | 风险事件 ID |
| `resource_type` | string | 资源类型 |
| `resource_id` | string | 资源 ID，可空 |
| `resource_ref` | string | 资源引用，必填 |

### 11.3 `risk.review_completed`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `risk_event_id` | string | 风险事件 ID |
| `resource_type` | string | 资源类型 |
| `resource_id` | string | 资源 ID；当 `resource_type=order` 时必须等于 `order_id` |
| `resource_ref` | string | 资源引用，必填 |
| `decision` | string | 最终复核结论 |
| `workflow_id` | string | 关联工作流 ID，可空 |
| `idempotency_key` | string | 原始请求幂等键，可空 |
| `resume_token` | string | 恢复引用令牌，可空 |

分区约束：若复核目标为订单，`partition_key` 必须等于 `resource_id/order_id`。

### 11.4 `notification.send_requested`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `notification_type` | string | 通知类型 |
| `business_key` | string | 业务去重键，可空 |
| `idempotency_key` | string | 集成命令幂等键 |
| `target_ref` | string | 目标引用 |
| `channel` | string | 目标渠道 |
| `payload` | object | 通知内容 |

说明：该事件是集成命令事件，可由业务域发布，`notification-service` 消费后落通知主记录。

### 11.5 `notification.created`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `notification_id` | string | 通知 ID |
| `notification_type` | string | 通知类型 |
| `target_ref` | string | 目标引用 |
| `channel` | string | 通知渠道 |
| `status` | string | 当前状态，初始为 `CREATED` |

### 11.6 `notification.delivered`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `notification_id` | string | 通知 ID |
| `channel` | string | 投递渠道 |
| `delivered_at` | string(datetime) | 投递完成时间 |

## 12. 工作流事件字段

适用于 `workflow.events` 主题：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `workflow_id` | string | 工作流实例 ID |
| `workflow_type` | string | 工作流类型 |
| `activity_name` | string | 当前活动名称 |
| `attempt` | integer | 当前重试次数 |
| `status` | string | 工作流或活动状态 |
| `reason_code` | string | 失败原因码，可空 |

## 13. 回放与死信字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `meta.replay` | boolean | 是否来自回放 |
| `meta.dlq` | boolean | 是否来自死信队列 |
| `meta.retry_count` | integer | 累计重试次数 |
| `meta.first_failed_at` | string(datetime) | 首次失败时间 |
| `meta.last_failed_at` | string(datetime) | 最近失败时间 |
| `meta.reason_code` | string | 失败原因码 |

## 14. 使用规则

- 新增事件类型时，必须先定义信封字段、payload 字段、分区键，再进入模块文档。
- 同一字段如已在 `API字段字典.md` 定义，语义必须保持一致，不得另起一套命名。
- 事件 payload 中不应重复塞入无关字段，尽量围绕聚合根和消费目的最小化设计。
- 若事件字段语义变更，必须同步修改事件 schema、模块文档和回放/审计逻辑。
