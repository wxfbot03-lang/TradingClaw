# TradingClaw 智能能力与治理详细设计

## 1. 文档定位

- 本文档覆盖 `ai-orchestration-service`、`ocr-service`、`capability-registry-service`，定义 AI 编排、OCR 识别和平台能力治理的统一设计。
- 本模块为问答增强、工具调用、验证码识别和跨端能力开放控制提供基础支撑。
- AI 和 OCR 只提供辅助能力与编排能力，不直接持有交易主链路状态，也不能越权执行真实交易写操作。

## 1.1 相关文档

- 总体总览：`docs/详细设计/service/后端详细设计.md`
- API 字段字典：`docs/详细设计/service/API字段字典.md`
- 错误码字典：`docs/详细设计/service/错误码字典.md`
- 状态字段枚举表：`docs/详细设计/service/状态字段枚举表.md`
- 事件字段字典：`docs/详细设计/service/事件字段字典.md`
- 网关与平台基础：`docs/详细设计/service/网关与平台基础详细设计.md`
- 行情与资讯：`docs/详细设计/service/行情与资讯详细设计.md`
- 交易网关：`docs/详细设计/service/交易网关详细设计.md`
- 策略系统：`docs/详细设计/service/策略系统详细设计.md`
- 风控审计与通知：`docs/详细设计/service/风控审计与通知详细设计.md`

## 2. 模块定位

### 2.1 `ai-orchestration-service`

- 负责问答编排、模型配置读取、工具调用和上下文聚合。
- 负责将行情、资讯、策略和交易等外部上下文组织为可控的 AI 输入。

### 2.2 `ocr-service`

- 负责验证码识别和 OCR 任务标准化结果输出。
- 负责 OCR 任务记录、结果追踪和错误返回。

### 2.3 `capability-registry-service`

- 负责跨端能力版本矩阵、开放状态、降级策略和工具可见性控制。
- 负责为网关、前端、CLI 和 AI 提供统一能力开放口径。

## 3. 领域边界

- AI 只负责编排、建议和增强，不直接持有交易订单、策略状态或账户事实。
- OCR 只负责识别，不负责券商登录或交易认证主状态机。
- 能力注册中心只维护能力元数据和开放治理，不维护业务主状态。
- 平台能力开放状态统一使用 `capability_status`，不得与账户域 `account_capability_status` 混用。

## 4. 核心对象

| 对象 | 说明 |
| --- | --- |
| `ModelConfig` | 模型配置 |
| `Conversation` | AI 问答会话 |
| `ToolCall` | 工具调用记录 |
| `OCRJob` | OCR 任务 |
| `CapabilityDefinition` | 能力定义 |
| `CapabilityVersion` | 能力开放版本与降级策略 |

## 5. 智能能力主线

### 5.1 AI 问答与工具调用主线

流程如下：

1. 接收用户问题。
2. 查询 `capability-registry-service`，获取当前客户端和版本允许使用的工具与能力。
3. 聚合行情、资讯、策略、交易等上下文。
4. 在入模前执行脱敏、权限裁剪和最小必要字段过滤。
5. 调用模型并执行工具链。
6. 输出结果，并发布工具调用审计事件。

治理约束：

- 必须明确首批模型供应商、超时阈值、token 限额、单次工具调用预算和失败回退策略。
- 涉及交易、账户和 OCR 的上下文在入模前必须执行脱敏和最小必要字段裁剪。
- AI 结果可解释，但不可越权执行交易写操作。

### 5.2 OCR 识别主线

流程如下：

1. 接收图片引用或 OCR 任务请求。
2. 调用 OCR 供应商执行识别。
3. 返回标准化文本、错误码和置信度。
4. 发布 `ocr.job_finished` 事件。

治理约束：

- 必须明确首批 OCR 供应商、主备 fallback、单次超时和重试上限。
- 验证码图像和识别文本必须按敏感信息策略设置保留期和脱敏边界。

### 5.3 平台能力治理主线

流程如下：

1. 维护能力 ID、开放版本、跨端可见性和降级策略。
2. 为网关和 AI 工具调用提供能力判断接口。
3. 对未开放能力返回统一降级说明。

治理约束：

- `enabled` 固定表示配置是否启用；开放态、降级态等扩展语义统一使用 `capability_status`。
- 能力注册中心必须成为前端、CLI、AI 和网关的统一版本口径来源。

## 6. 数据设计

核心表：

- `model_configs`
- `ai_conversations`
- `ai_tool_calls`
- `ocr_jobs`
- `capability_definitions`
- `capability_versions`

设计要点：

- 模型配置、AI 会话、工具调用记录、OCR 任务、能力定义与版本矩阵建议落 MySQL，便于审计和版本回溯。
- 大模型上下文长文本、OCR 原图和工具调用原始输入输出建议存对象存储，MySQL 只保留索引和摘要。
- Redis 可用于短期会话上下文缓存、能力查询热点缓存和工具调用去重键，但不能替代会话与审计主记录。
- 默认值建议：`ai_conversations.status = ACTIVE`，`ai_tool_calls.status = PENDING`，`ocr_jobs.status = CREATED`，`capability_versions.enabled = false`。
- AI 会话和工具调用默认只做归档或脱敏，不直接物理删除；能力版本通过 `enabled` 和版本切换管理下线。
- 模型配置、能力版本和工具调用记录建议补 `created_by`、`updated_by`、`provider_code`，便于回溯供应商和操作者来源。
- MVP 阶段允许 `ai-orchestration-service`、`ocr-service`、`capability-registry-service` 逻辑分层、物理合并部署，稳定后再独立扩展。

### 6.1 `ai_conversations`

| 字段 | 类型建议 | 约束/索引 | 说明 |
| --- | --- | --- | --- |
| `id` | bigint / uuid | PK | 主键 |
| `conversation_id` | varchar(64) | UK | AI 会话 ID |
| `user_id` | varchar(64) | idx | 用户 ID |
| `model_id` | varchar(64) | idx | 模型 ID |
| `context_scope` | json |  | 上下文范围 |
| `status` | varchar(32) | idx | 会话状态 |
| `last_message_at` | datetime | idx | 最近消息时间 |
| `created_at` | datetime | idx | 创建时间 |

### 6.2 `ai_tool_calls`

| 字段 | 类型建议 | 约束/索引 | 说明 |
| --- | --- | --- | --- |
| `id` | bigint / uuid | PK | 主键 |
| `conversation_id` | varchar(64) | idx | AI 会话 ID |
| `tool_name` | varchar(64) | idx | 工具名称 |
| `resource_ref` | varchar(255) | idx | 资源引用 |
| `status` | varchar(32) | idx | 调用状态 |
| `request_ref` | varchar(255) |  | 请求对象引用 |
| `response_ref` | varchar(255) |  | 响应对象引用 |
| `created_at` | datetime | idx | 创建时间 |

### 6.3 `ocr_jobs`

| 字段 | 类型建议 | 约束/索引 | 说明 |
| --- | --- | --- | --- |
| `id` | bigint / uuid | PK | 主键 |
| `job_id` | varchar(64) | UK | OCR 任务 ID |
| `image_ref` | varchar(255) |  | 图片引用 |
| `scene` | varchar(64) | idx | OCR 场景 |
| `provider_code` | varchar(64) | idx | 供应商编码 |
| `status` | varchar(32) | idx | 任务状态 |
| `confidence` | decimal(10,4) |  | 识别置信度，可空 |
| `reason_code` | varchar(64) | idx | 失败原因码，可空 |
| `finished_at` | datetime | idx | 任务完成时间，可空 |
| `text_ref` | varchar(255) |  | 识别结果引用 |
| `created_at` | datetime | idx | 创建时间 |

### 6.4 `capability_versions`

| 字段 | 类型建议 | 约束/索引 | 说明 |
| --- | --- | --- | --- |
| `id` | bigint / uuid | PK | 主键 |
| `capability_id` | varchar(64) | UK(capability_id, open_version) | 能力 ID |
| `open_version` | varchar(32) | UK(capability_id, open_version) | 开放版本 |
| `enabled` | boolean | idx | 是否启用 |
| `capability_status` | varchar(32) | idx | 能力开放状态 |
| `degrade_strategy` | varchar(64) |  | 降级策略 |
| `client_matrix` | json |  | 客户端可见性矩阵 |
| `updated_at` | datetime | idx | 更新时间 |

## 7. 事件设计

核心事件：

- `conversation.created`
- `tool_call.executed`
- `ocr.job_finished`

事件约束：

- 工具调用事件应保留资源引用与结果状态，供审计和追踪使用。
- OCR 事件只表达识别任务完成结果，不表达业务认证主流程状态。

## 8. 接口设计

### 8.1 HTTP 入口

- `/api/v1/ai/chat`
- `/api/v1/ai/models`
- `/api/v1/ocr/jobs`
- `/api/v1/ocr/jobs/{id}`
- `/api/v1/capabilities`

#### 8.1.1 `POST /api/v1/ai/chat`

必需请求头：`Authorization`

请求体：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `conversation_id` | string | 否 | 不传则创建新会话 |
| `question` | string | 是 | 用户问题 |
| `context_scope` | array | 否 | 如 `market`、`news`、`strategy`、`trade` |
| `tool_preference` | array | 否 | 允许优先使用的工具 |

返回体 `data`：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `conversation_id` | string | 会话 ID |
| `answer` | string | 最终回答 |
| `tool_calls` | array | 工具调用摘要 |
| `model_name` | string | 使用模型 |

语义约束：

- AI 聊天属于同步增强接口，完成态以 `conversation_id` 为主键观测，不额外返回 `workflow_id`。

#### 8.1.2 `GET /api/v1/ai/models`

返回体 `data`：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `items` | array | 模型列表 |
| `items[].model_id` | string | 模型 ID |
| `items[].provider_code` | string | 提供方 |
| `items[].enabled` | boolean | 是否启用 |

#### 8.1.3 `POST /api/v1/ocr/jobs`

必需请求头：`Authorization`、`X-Idempotency-Key`

请求体：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `image_ref` | string | 是 | 图片引用或上传返回 ID |
| `scene` | string | 是 | 如 `broker_captcha` |

返回体 `data`：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `job_id` | string | OCR 任务 ID |
| `status` | string | 任务状态 |

语义约束：

- 该接口为任务型资源创建接口，创建时只保证任务已登记。
- 最终识别结果通过查询接口获取，不在创建响应中混放最终结果字段。

#### 8.1.4 `GET /api/v1/ocr/jobs/{id}`

路径参数：`id` 为 OCR 任务 ID。

返回体 `data`：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `job_id` | string | OCR 任务 ID |
| `status` | string | 任务状态 |
| `text` | string | 识别结果，可空 |
| `confidence` | number | 置信度，可空 |
| `reason_code` | string | 失败原因码，可空 |

#### 8.1.5 `GET /api/v1/capabilities`

查询参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `client_type` | string | 否 | `web`、`cli`、`ai` |
| `version` | string | 否 | 客户端版本 |

返回体 `data`：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `items` | array | 能力列表 |
| `items[].capability_id` | string | 能力 ID |
| `items[].enabled` | boolean | 是否启用 |
| `items[].capability_status` | string | 能力开放状态 |
| `items[].open_version` | string | 开放版本 |
| `items[].degrade_strategy` | string | 降级策略 |

### 8.2 工作流

- `AIToolCallingWorkflow`

## 9. 实施要点

- AI 编排接口建议使用 FastAPI + Pydantic 实现，请求上下文、工具调用结果和能力配置统一通过 Python 数据模型收口。
- 工具调用工作流建议复用 Temporal Python SDK，避免 AI 编排、OCR 任务和能力治理各自维护独立后台任务框架。
- AI 工具调用必须经过工具白名单、资源权限校验和能力开放判断。
- AI 可以提供建议和解释，但不能绕过交易风控直接修改交易事实。
